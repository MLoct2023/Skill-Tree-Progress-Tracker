# Skill Tree Progress Tracker

This project is a gamified application to track skill progression using React.js and Firebase.

## How to Run
1. Navigate to the `frontend` folder and run `npm install`.
2. Navigate to the `backend/functions` folder and run `npm install`.
3. Start the frontend with `npm start`.
4. Deploy backend with Firebase CLI.